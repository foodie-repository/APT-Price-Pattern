[project]
name = "apt-price-pattern"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.4.0",
    "numpy>=2.3.0",
    "pandas>=2.3.0",
]
